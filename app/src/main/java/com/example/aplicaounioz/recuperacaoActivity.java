// RecuperacaoSenhaActivity.java
package com.example.aplicaounioz; // Mude para o seu pacote

import androidx.appcompat.app.AppCompatActivity;
import android.os.Bundle;

public class recuperacaoActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_recuperacao);

        // Coloque aqui a lógica de envio de e-mail ou redefinição de senha.
    }
}